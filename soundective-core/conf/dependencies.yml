# Module dependencies

self: soundective -> soundective-core 0.0.1

require:
    - play 1.2
    - play -> scala 0.9
    - mp3agic -> mp3agic 0.6

repositories:
    - mavenLocal:
        type: iBiblio 
        root: file://${user.home}/.m2/repository/
        contains:
           - mp3agic -> *
